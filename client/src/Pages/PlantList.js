import React from "react";

function PlantList(){
//     const [plants, setPlants] = useState([]);

//   useEffect(() => {
//     fetch("/plants")
//       .then((r) => r.json())
//       .then((plants) => setPlants(plants));
//   }, []);

    return(
        <div>
            <p>hold for plant cards</p>
        </div>
    )
}

export default PlantList;